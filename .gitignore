# Data never lives in git. It lives on Drive, fetched from EDI inside Colab.
data/
!tests/fixtures/

# Figures are results. They are committed only once produced from the full
# dataset in Colab, never from the 600-row test fixture, which would present
# fixture noise as a finding.
reports/figures/*.png
reports/tables/*.csv

# Working notes, not part of the deliverable
PLAN.md
*.docx

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/
.venv/

# Earth Engine / Google credentials
*.json
!notebooks/*.ipynb
.ee_credentials
